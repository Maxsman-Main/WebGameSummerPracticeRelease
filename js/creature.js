class Creature {
    
    /**
     * 
     * @param {number} name
     * @param {image} sprite
     * @param {string} label some char, used for debugging
     */
    constructor(name, sprite, label) {
        this.name = name;
        this.sprite = sprite;
        this.label = label;
    }
}